/*
import 'package:flutter/material.dart';

class CustomScaffold extends StatelessWidget {
  const CustomScaffold({super.key, this.BodyViewMobile, this.BodyViewWeb, this.color});

  final Widget? BodyViewWeb;
  final Widget? BodyViewMobile;
  final Color? color;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: color ?? Colors.grey.shade100,
      BodyView: (MediaQuery.of(context).size.width < 500) ? BodyViewMobile : BodyViewWeb,
    );
  }
}
*/
